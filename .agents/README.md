# Workspace Customizations

This directory contains workspace-specific customizations for Antigravity, such as custom skills and rules.

## Directory Structure
- `skills/`: Place custom skills here. Each skill should be a subfolder containing a `SKILL.md` file.
- `AGENTS.md`: (Optional) Add project-scoped behavior rules here.
